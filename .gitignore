node_modules
.next
out
build
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env
.env.local
.env*.local
.vercel
next-env.d.ts
tsconfig.tsbuildinfo
.next-verify/

# Original standalone ambassador app — hardcoded admin fallback password and
# real ambassador names. Its history was scrubbed from this repo after it was
# committed by mistake (see git log for the filter-repo rewrite). Keep any
# copy of it outside this repo, e.g. ~/Downloads/Educraft_Ambassador_reference/
# — never inside a folder this .gitignore doesn't cover.
Educraft_Ambassador/

# Traqly source copy (reference only, never commit; pull analytics into src/ instead)
/Traqly/

.vercel

# Private files kept on disk by the local development driver (PRIVATE_FILES_DRIVER=local)
.private-files/

# Playwright MCP test artifacts (console logs, page snapshots)
.playwright-mcp/

# Throwaway QA scripts (live checks with test logins) — never committed
scripts/tmp/
